{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": { "cache": false, "persistent": true },
    "build": { "outputs": [".next/**", "dist/**", "android/**", "ios/**"] },
    "lint": { "outputs": [] },
    "test": { "outputs": ["coverage/**"] }
  }
}
